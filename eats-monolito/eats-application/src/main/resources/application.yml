spring:
  config:
    import: configserver:http://config-server:8888
  cloud:
    config:
      enabled: true
  application:
    name: eats-monolito
  profiles:
    active: default
    config:
      uri: http://config-server:8888