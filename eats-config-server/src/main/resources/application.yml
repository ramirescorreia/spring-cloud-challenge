server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pbarros1979/spring-cloud-challenge-config.git
          basedir: ../../spring-cloud-challenge-config
          cloneOnStart: true
          skipSslValidation: true
          default-label: main
           
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka