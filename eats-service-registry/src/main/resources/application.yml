eureka:
  client:
    fetchRegistry: false
    registerWithEureka: false
    serviceUrl:
      defaultZone: http://host.docker.internal:${server.port}/eureka/
  instance:
    hostname: localhost
server:
  port: 8761
